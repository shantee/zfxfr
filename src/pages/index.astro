---
import MainLayout from "../layouts/MainLayout.astro";
import { getCollection } from "astro:content";
import Map from "../components/Map.astro";
const all = await getCollection("posts", ({ data }) => !data.draft);
const getTime = (d)=> new Date(d.published ?? d.date ?? d.publishDate ?? d.updated ?? 0).getTime();
const posts = all.sort((a,b) => getTime(b.data) - getTime(a.data));

const readMeta = (d) => {
  const words = d.wordCount ?? d.readingTime?.words ?? 0;
  const minutes = Math.max(1, Math.round(d.readingTime?.minutes ?? (words ? words/200 : 1)));
  return { time: minutes, wordCount: words };
};
---

<MainLayout bannerHeight="40svh" seoTitle="Dépannage informatique à Vesoul / Pusey"  seoDescription="Assistance, réparation PC, et coaching numérique à domicile autour de Vesoul / Pusey."
 >
  <div class="article-wrapper">
<h1 class="mb-4 text-2xl font-extrabold text-gray-900 dark:text-white md:text-3xl lg:text-4xl">

  <span class="text-transparent bg-clip-text bg-gradient-to-r to-lime-600 from-blue-400">
    Dépannage / assistance informatique à domicile
  </span>
<span class="text-blue-500 dark:text-blue-300">autour de Vesoul.</span>
</h1>

<p class="text-lg font-normal text-gray-700 lg:text-xl dark:text-gray-400">
  PC trop lent ? Mot de passe oublié ? Wi-Fi capricieux ? Imprimante récalcitrante ?
  Photos et documents en bazar, besoin d’une sauvegarde propre ? Nouveau smartphone à apprivoiser ?
</p>

<p class="mt-4 text-lg font-normal text-gray-700 lg:text-xl dark:text-gray-400">
  <span class="inline-block rounded-lg bg-blue-50 px-2 py-1 font-semibold text-blue-700 dark:bg-blue-950 dark:text-blue-300">Prenez rendez-vous
  </span>
  par téléphone au <span class="font-semibold">06.51.15.57.58</span> ou <span class="inline-block rounded-lg bg-blue-50 px-2 py-1 font-semibold text-blue-700 dark:bg-blue-950 dark:text-blue-300">contactez moi</span> via le <a
    href="/contact/"
    title="Prenez rendez-vous via le formulaire de contact"
    class="font-semibold text-blue-600 underline underline-offset-4 decoration-blue-300 hover:text-blue-700 hover:decoration-blue-500
           focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-400 focus-visible:ring-offset-2
           dark:text-blue-400 dark:decoration-blue-700 dark:hover:text-blue-300 dark:hover:decoration-blue-400 dark:focus-visible:ring-blue-500"
  >
    formulaire de contact.
  </a>
</p>

<p class="mt-4 text-lg font-normal text-gray-700 lg:text-xl dark:text-gray-400">
  Je <span class="font-semibold">dépanne</span>, je prends le temps d’<span class="font-semibold">expliquer</span>,
  je réponds à vos <span class="font-semibold">questions</span>… et je vous laisse avec un truc qui marche !
</p>

<p class="mt-4 text-lg font-normal text-gray-700 lg:text-xl dark:text-gray-400">
  Et si vous voulez aller plus loin, je propose aussi de l’
  <span class="font-semibold">assistance / coaching numérique</span> ainsi que des{" "}
  <a
    href="/services"
    title="Cours, formations et services"
    class="font-semibold text-blue-600 underline underline-offset-4 decoration-blue-300 hover:text-blue-700 hover:decoration-blue-500
           focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-400 focus-visible:ring-offset-2
           dark:text-blue-400 dark:decoration-blue-700 dark:hover:text-blue-300 dark:hover:decoration-blue-400 dark:focus-visible:ring-blue-500"
  >
    cours complets
  </a>.
</p>

<p class="mt-3 text-lg font-normal text-gray-700 lg:text-xl dark:text-gray-400">
  Pour toute question, n’hésitez pas à me{" "}
  <a
    href="/contact/"
    title="Prenez rendez-vous via le formulaire de contact"
    class="font-semibold text-blue-600 underline underline-offset-4 decoration-blue-300 hover:text-blue-700 hover:decoration-blue-500
           focus:outline-none focus-visible:ring-2 focus-visible:ring-blue-400 focus-visible:ring-offset-2
           dark:text-blue-400 dark:decoration-blue-700 dark:hover:text-blue-300 dark:hover:decoration-blue-400 dark:focus-visible:ring-blue-500"
  >
    contacter
  </a>
  !
</p>
<h2 class="mt-10 mb-3 text-2xl font-extrabold tracking-tight text-blue-400 dark:text-gray-300 md:text-3xl">
  Basé à <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-400 to-lime-600">Pusey (70000)</span>
</h2>

<p class="text-lg font-normal text-gray-700 dark:text-gray-400 lg:text-xl">
  Je peux vous accueillir <span class="inline-block rounded-md bg-blue-50 px-2 py-0.5 font-semibold text-blue-700 dark:bg-blue-950 dark:text-blue-300">sur rendez-vous</span>.
  Sinon, je me déplace à domicile dans un rayon d’environ <span class="font-semibold">15 km</span> autour de Vesoul / Pusey.
</p>
<br><br>
<Map lat={47.65296} lng={6.12814} zoom={12} minZoom={11} radiusKm={10}
     popup="Autour de Vesoul (10 km)" clickToWheelZoom />
<br><br>


  </div>
      
</MainLayout>

<style>
  .HomeLatest { width:100%; max-width:var(--page-width-xl); margin-inline:auto; padding-inline:16px; }
  .list { display:grid; gap:2rem; }
  .title {font-size: 2.2rem;}
  .title:before {position: relative;
  display: block;
  content: " ";
  background-color: var(--primary-color);
  top: 2.5px;
  left: -15px;
  bottom: 3px;
  width: 0.3rem;
  border-radius: 4px;}
</style>
<style is:global>
    .main-container {
      max-width: 100% !important;
      margin: 0 0 0 0px !important;
      padding-left: 0px !important;
      padding-right: 0px !important;
    }
  .main-container > .flex.flex-row.items-start > .space-y-8 {
    flex: 1 1 auto;
    min-width: 0;
  }
   .article-wrapper {
    @apply mx-3 rounded-2xl bg-[var(--card-color)] px-5 py-6 lg:mx-0 lg:px-10 lg:py-9;
  }
  .article {
    @apply flex flex-col;
    font-size: var(--primary-font);
  }
</style>
